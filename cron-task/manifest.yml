applications:
- name: task-runner
  buildpacks:
  - nodejs_buildpack
  disk_quota: 1G
  instances: 1
  memory: 256M
  command: (npm start && echo SUCCESS || echo FAIL) && sleep infinity
  health-check-type: none
  no-route: true

